//test return 80

//15411 interface

int main(){
    fpt x = 12345;
    fpt res = 0;
    fpt y = 10;
    while(fdiv(x, y) > 0){
        fadd(res, fmul(y, 2));
        x = fdiv(x, y);
    }
    return res;
}